# thirdparty/imgui/CMakeLists.txt

add_library(imgui
    imgui.cpp
    imgui_draw.cpp
    imgui_tables.cpp
    imgui_widgets.cpp
    imgui_demo.cpp #remove if you don't need the demo.
    backends/imgui_impl_win32.cpp
    # add other imgui files here.
)

target_include_directories(imgui PUBLIC .) #make sure that the imgui headers are available.
